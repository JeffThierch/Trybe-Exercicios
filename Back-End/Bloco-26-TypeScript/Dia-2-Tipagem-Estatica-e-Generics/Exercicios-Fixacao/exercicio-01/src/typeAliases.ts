type Bird = {
  color: string,
  race: string,
  name?: string,
  age?: number
}

type Sum = (num1 : number, num2: number) => number;

type Address = {
  street: string,
  number: number,
  city: string,
  state: string
  country: string,
}